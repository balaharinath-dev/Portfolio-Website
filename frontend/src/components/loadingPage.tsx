import React, { useEffect, useState } from 'react'
import styles from "../utilities/loading.module.css"

var Loading:React.FC=()=>{

    return (
        <div className={`${styles.center}`}>
            
        </div>
    )
}

export default Loading